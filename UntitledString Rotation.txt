
#include<bits/stdc++.h>
using namespace std;
#define ll long long int

void solve(){
  string s1,s2,S;
  cin>>s1>>s2;
  S=s1.append(s1);
  cout<<(S.contains(s2)?"Yes":"No")<<endl;
}

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  int T=1;
  //cin>>T;
  while(T--){
    solve();
  }
}
