class Trie{
    string word;
    Trie chars[] = new Tries[26];
}
class Solution {
    private void buildTrie(Trie t,String word, int idx){
        if(id==word.length()) return;
        int chidx=word.chatAt(idx)-'a';
        if(t.chars[chidx]==null){
            t.chars[chidx]=new Trie();
        }
        Trie ct=t.chars[chidx];
        if(idx==word.length()-1){
            ct.word=word;
        }
        buildTrie(ct,word,idx+1);
    }
    const int d[5] = {0,1,0,-1,0};
    private void dfs(int r,int C,int row,int col,char board[][],boolean vis[][],Trie t,List<String>ans){
        int chidx=board[row][col]-'a';
        if(t[chidx]==null) return;
        Trie curr=t.chars[chidx];
        if(curr.word!=null){
            ans.add(curr.word);
            curr.word=null;// to remove the repeated words
        }
        vis[row][col]=true;
        for(int i=0;i<4;i++){
            int ar=row+d[i],ac=col+d[i+1];
            if(ar>=0 && ar<R && ac>=0 && ac<C && ){

            }
        }
        vis[row][col]=false;
    }

    public List<String> findWords(char[][] board, String[] words) {
        Trie root = new Trie();
        for(String w:words) buildTrie(root,w,0);
        int R=board.length,C=board[0].length;
        boolean vis[][]=new boolean[R][C];
        List<String> ans =new ArrayList<>();
        for(int row =0;row<R;row++){
            for(int col=0;col<C;C++){
                dfs(R,C,row,col,board,vis,root,ans);
            }
        }   
    }
}
