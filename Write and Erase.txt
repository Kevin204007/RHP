#include<bits/stdc++.h>
using namespace std;
#define ll long long int

void solve(){
  int N;cin>>N;
  map<int,int> m;
  for(int i=0;i<N;i++) {
    int curr; cin>>curr;
    m[curr]++;
  }
  int ans=0;
  for (auto[num,cnt]:m) {
    if (cnt&1) ans++;
  }
  cout<<ans<<endl;
}

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  int T=1;
  //cin>>T;
  while(T--){
    solve();
  }
} 